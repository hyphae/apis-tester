[project]
name = "apis-tester"
version = "4.5.10"
description = "APIS Tester"
readme = "README.md"
requires-python = ">=3.12"

dependencies = [
    "bottle==0.13.4",
    "pytz==2026.1.post1",
    "six==1.17.0",
    "tornado==6.5.5"
]

[dependency-groups]
dev = [
    "ipython",
    "ruff",
]

[tool.uv]
package = false